# This is a crontab file for my account at NCF. Every day at noon,
# we remove every thesis backup that's more than a week old.
MAILTO=""
#NCF server has no mail(1)!
0 12 * * * echo `date` ": " >> $HOME/thesis-backups/cleanup.log; ls -t $HOME/thesis-backups/thesis-*.tbz2 | awk 'BEGIN {x=""}; NR > 7 {x=x"\n\t"$0; cmd="rm \"" $0 "\""; system(cmd)}; END {if (x != "") print "deleted:" x}' >> $HOME/thesis-backups/cleanup.log
