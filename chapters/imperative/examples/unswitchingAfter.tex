if (warnlevel >= 2000) {
    for (Person p = team.head; p->next != NULL; p = p->next) { 
        p.klaxon << "Warning!";
    }
} else {
    for (Person p = team.head; p->next != NULL; p = p->next) {
        p.spywatch << "All clear.";
    }
}